# python-k8s-agent
A simple Python web app that connects Google Gemini to a Kubernetes cluster via MCP. Query your cluster in natural language and let Gemini act as an agent to inspect or manage resources locally, without writing direct Kubernetes API code.
